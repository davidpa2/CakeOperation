class Cake {
    constructor(id, x, y, speed, sizeX, sizeY, number) {
        this.id = id;
        this.x = x;
        this.y = y;
        this.speed = speed;
        this.sizeX = sizeX;
        this.sizeY = sizeY;
        this.number = number;
    }
}