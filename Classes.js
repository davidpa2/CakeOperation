class Cake {
    constructor(id, x, y, speed, sizeX, sizeY, number, symbol) {
        this.id = id;
        this.x = x;
        this.y = y;
        this.speed = speed;
        this.sizeX = sizeX;
        this.sizeY = sizeY;
        this.number = number;
        this.symbol = symbol;
    }
}

class Button {
        constructor(x, y, width, height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}